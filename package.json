{
  "name": "subscription-manager",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start:client": "pnpm --filter client-subscription-manager dev",
    "start:server": "pnpm --filter server-subscription-manager dev",
    "dev": "pnpm --parallel dev",
    "install:all": "pnpm install"
  }
}
